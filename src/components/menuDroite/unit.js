/**
 * diplay unit of icons
 * @param {data} Unit for unit icons
 * @returns display unit icons
 */
export function Unit(data) {
    const unit = data.icon.unit
    return (
        <>
            {unit}
        </>
    )
}